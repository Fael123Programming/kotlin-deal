package classes

class Account(val number:String, val agency:String, val balance:Long){ //This is the primary constructor
    //Data class: all objects created can't have their data modified; 
    //When you create a primary constructor, you already define the attributes of this class
    fun deposit(amount:Long){

    }

    fun withdraw(amount:Long){

    }
}